%h1.page-header Payments owed

%table.std
  %thead
    %tr
      %th Company
      %th Payment owed
      %th Unpaid payments
      %th Total

  %tbody
    - payments.each do |payment|
      %tr
        %td= payment.company.name
        %td= number_to_currency payment.owed_paid
        %td= number_to_currency payment.owed_unpaid
        %td= number_to_currency payment.owed_total

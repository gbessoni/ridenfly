%h1.page-header Payments owed

%table.std
  %thead
    %tr
      %th Company
      %th Payment owed
      %th Unpaid payments
      %th Total

  %tbody
    - payments.each do |payment|
      %tr
        %td= payment.company.name
        %td= number_to_currency payment.owed_amount
        %td= number_to_currency payment.unpaid_amount
        %td= number_to_currency payment.total
